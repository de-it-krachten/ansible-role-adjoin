---

- name: Converge
  hosts: all
  vars:
    ad_password: test
    ad_realm: example.com
    ad_user: test
  tasks:
    - name: Include role 'ansible-role-adjoin'
      include_role:
        name: ansible-role-adjoin
